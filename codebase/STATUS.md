STATUS.md (running log)

2026-08-09 (pretrained TSFM pipeline, one-GPU serialized execution)

Done: Added `MODEL_EVALUATION_STANDARD.md`, seven-family model manifest, isolated-per-model Python 3.11 environment setup script, staging/smoke/run/submit CLI, and a serialized Slurm template that requests exactly one GPU and runs the seven models sequentially. Added unit coverage for manifest validation, sampling, resume markers, Slurm rendering, control-machine submission, and Chronos-Bolt’s current API. Chronos-Bolt Base checkpoint was staged locally and passed a real CPU synthetic-series smoke test. Its adapter was corrected for the installed `chronos-forecasting` prediction signature and quantile crossings.

Blocked: Current upstream `momentfm` and `timesfm` releases have incompatible NumPy pins, so the new per-model environments are required. Only Chronos-Bolt has completed a real local smoke run in this session; the remaining six still need their own environment/package/checkpoint smoke passes before GPU evaluation.

New packages/scripts added: local untracked Chronos-Bolt smoke environment and checkpoint cache; both ignored by git. New tracked scripts/configuration are listed above.

Next session should start with: create the remaining six isolated environments using `scripts/setup_model_envs.sh`, stage their checkpoints, run `run_pretrained_models.py smoke --model <name> --device cpu`, then copy/fill `config/slurm.example.yaml` as `config/slurm.yaml` on the RTX server and render the submission script.

Append a new entry at the top after every work session, newest first. Keep entries short and factual: what got done, what’s blocked, what’s next. This is the main way continuity survives across sessions and across the move to the collaborator’s compute/storage.

2026-07-08 (final two bridge-phase items: Traffic dedup check + lab directory inventory — bridge phase genuinely closed)

The two items flagged as the natural next steps in the last two entries, both resolved. This really does close out the bridge phase — nothing scoped for this phase remains open.

Item 1: Traffic vs. Monash traffic_hourly — CONFIRMED BYTE-IDENTICAL. traffic_hourly isn’t part of this project’s chosen 20-dataset Monash subset, so it was fetched directly from its own Zenodo record (10.5281/zenodo.4656132) into scratch — not added to datasets/raw/, purely a verification artifact — and parsed with the same gluonts TSFReader used elsewhere. Both this project’s traffic.parquet and Monash’s traffic_hourly are exactly 862 series of exactly 17,544 hourly steps. Correlated all 862×862 pairs at zero offset (no sliding-window search needed, unlike Electricity’s length mismatch): every one of Monash’s 862 series is the top match for the identically-indexed sensor in our own Traffic dataset (traffic__0↔T1, etc., no reordering), all at correlation ≥0.99999, max absolute value difference across all 862×17,544 points = 2.98e-08 (float rounding noise, not a real difference). This lands in the same category as the Solar triplicate (byte-identical), not Electricity’s looser “same population, different pipeline” category — actually a cleaner match than either, since there’s no aggregation difference or scale mismatch to account for. Also found the same date-mislabeling pattern already documented for Electricity: Monash’s copy is correctly dated 2015-2016 (matching its own description and the Lai et al. 2017 source), while this project’s own copy — same thuml/Time-Series-Library redistribution lineage as Electricity — is dated 2016-2018 despite identical values. This is now the second instance of that specific redistribution mislabeling dates on otherwise-legitimate data, a pattern worth remembering rather than two unrelated coincidences. Full method, numbers, and the contamination conclusion (ties this project’s Traffic dataset, Monash’s traffic_hourly, and — per Meyer et al. 2025’s already-cited claim — TimesFM’s pretraining corpus into one confirmed chain) are in traffic.md and monash.md.

Item 2: the unnamed existing server asset — inventoried, “engauge” itself still not found. The deferral on Emotiv, Brain, SensorLLM, GlycoLM, TSZechen was lifted and all five were actually opened and inspected (real file listings and sizes, not folder-name guessing), matching the original Step 1 standard. All five are legitimate research data in domains this project already excludes this phase: Emotiv (188GB) and Brain (882GB) are EEG/brain-decoding research (published corpora: Brennan & Hale’s naturalistic-reading EEG, the OpenNeuro CHISCO imagined-speech dataset, plus a custom lab EEG experiment and EEG-to-text model code); SensorLLM (8GB) and GlycoLM (65GB) are CGM (continuous glucose monitoring) + LLM reasoning work, built on PhysioNet’s genuine open-access “BIG IDEAs” glycemic+wearable dataset (16 subjects, standard PhysioNet distribution — same style as WESAD/PTB-XL already in this project’s scope, just a different domain); TSZechen (2.0TB, by far the largest) is Human Activity Recognition + LLM fusion research built on several well-known published HAR benchmarks (UCI HAR, PAMAP2, OPPORTUNITY, Capture-24, HHAR, USC-HAD, WISDM, Meta’s Nymeria). None of this overlaps this project’s Minimal/Recommended tier scope. “engauge” was searched for explicitly (case-insensitive, including near-miss spellings) across all five directories and genuinely not found — no directory, file, or dataset name found is a plausible match. This is left as unresolved, not force-fit to anything, per instruction. EXISTING_ASSETS.md rewritten with the full per-directory breakdown; DATASETS.md’s deferred-assets table and CLAUDE.md’s known-open-items section updated to match.

One thing paused for mid-session, per instruction: found /workspace/SensorLLM/vaibhav/FOOD_CGM/.env while inventorying SensorLLM — a filename that conventionally holds credentials/API keys. Did not open it. Stopped and asked before proceeding; confirmed to skip it and continue the rest of the inventory around it, which is what happened. Nothing about its contents is documented anywhere in this project.

Blocked: None.

New packages/scripts added: None. One dataset fetched to scratch only for verification (Monash’s traffic_hourly, not added to datasets/raw/ or this project’s tracked Monash subset).

This is the real end of the bridge phase. Every dataset in DATASETS.md is [x], both prior sessions’ open items are resolved, the citation-accuracy correction pass is done, and the two final flagged follow-ups (Traffic dedup, lab directory inventory) are both closed out. Next session starts only once real compute and storage from the collaborator lands — there is no more bridge-phase work queued.

2026-07-08 (correction pass on Step 5 literature-confirmation citations — bridge phase re-closed)

A small correction pass, not a new step, on top of the already-closed bridge phase. An external review (not this project’s own agents) checked three of last session’s literature-confirmation claims against the actual paper text and found two citation-accuracy issues plus one lead worth strengthening. All three were verified directly against the fetched paper text (arxiv.org/html/2510.13654, Meyer et al. 2025) before editing anything, per instruction not to take the correction request as gospel.

1. ETTh1.md / ETTh2.md / ETTm1.md — fixed a misattributed quote. The external review was correct on both counts. The paper’s real sentence is a close paraphrase of what these cards presented as verbatim (“the Informer collection… contains individual series such as ETTh1, ETTh2, and ETTm1 that have also been used for pre-training (e.g., in Lag-Llama) or train/test evaluation (e.g., in UniTime)” vs. the cards’ “some individual time series of the Informer collection… have also been used for pre-training…”). More importantly, the “concluding… cannot be benchmarked against any other model using the Informer collection” sentence does not exist in the paper attached to ETT at all — that phrasing (“making it practically useless for any cross-model comparison”) is used elsewhere in the paper for a different dataset, Monash’s Australian Electricity Demand (used across Lag-Llama/Timer pretraining, Moirai train/test, and Chronos/TimesFM zero-shot). Reworded all three cards: the underlying finding (ETTh1/h2/m1 named as used in Lag-Llama pretraining and UniTime train/test) is kept, now presented as an accurate paraphrase with the real sentence quoted, and the misattributed conclusion is removed with a note explaining where that stronger language actually comes from in the paper.

2. electricity.md — corrected an understated “nothing found” claim. The external review was correct. The card said no literature source addresses this dataset by name for cross-collection duplication. Confirmed against the actual paper text that this is wrong: Meyer et al. 2025 states verbatim that “‘ElectricityLoadDiagrams20112014’ appears under the name ‘Electricity’ in both the Autoformer and Monash collections… and as ‘ECL’ in the Informer collection” — exactly this project’s own dataset. Updated the card to credit that citation for the cross-collection-duplication finding in general, while keeping the two things that are still genuinely this project’s own original contribution: the specific correlation-based GIFT-Eval numbers (not mentioned in that paper) and the date-mislabeling finding (still uncorroborated anywhere).

3. traffic.md — strengthened an existing open item with a named citation. The external review’s lead checked out. Confirmed verbatim: Meyer et al. 2025 states “a potential overlap surfaced between the pre-training and test set in TimesFM… as the traffic hourly dataset appears to have been used during pretraining while also being included in the reported Monash evaluation benchmark.” Added this to the card’s existing (still-unverified) note about this project’s own Traffic dataset likely being the same series as Monash’s traffic_hourly — the paper’s claim is specifically about Monash’s own copy and TimesFM, not directly about this project’s copy, so the card is careful to say the byte-level identity still needs the same correlation check already deferred to Phase 1, not claim the paper proves overlap with our copy directly. This just gives whoever picks that check up next a concrete model and citation to anchor to instead of a structural hunch alone.

What did NOT need to change: the underlying facts in all three cards (ETTh1/h2/m1 named in Lag-Llama/UniTime, Electricity/ECL’s cross-collection duplication, Traffic’s likely identity with traffic_hourly) were all correct — only the precision of how they were sourced/quoted needed fixing. No new byte-level checks were run this session (the Traffic-vs-traffic_hourly correlation check stays deferred to Phase 1, as already planned).

Blocked: None.

New packages/scripts added: None.

This closes out the bridge phase for real, again. Next session picks up only once real compute and storage from the collaborator lands, or — if there’s another bridge session first — with the Traffic-vs-traffic_hourly correlation check and the still-unnamed existing server asset as the two natural starting points, per the previous entry’s own notes.

Entry template

## <date>
**Done:**
-

**Blocked:**
-

**New packages/scripts added:**
-

**Next session should start with:**
-
2026-07-08 (Step 5 + Step 6: literature confirmation and phase wrap-up — BRIDGE PHASE COMPLETE)

This closes out the whole bridge phase. All 14 datasets are [x] in DATASETS.md: downloaded, standardized, EDA’d, and now literature-confirmed with a full card in literature/notes/. Two gaps flagged at the start of this session were fixed first (missing GIFT-Eval solar EDA plot; STEPS.md checkboxes not reflecting completed work), then Step 5 ran as 8 parallel research agents (one per dataset family, real WebSearch/WebFetch against primary sources — papers and reference-implementation data loaders, not memory), then Step 6.

Every dataset’s final state:

ETTh1/h2/m1/m2: split (Informer’s fixed 12/4/4-month) and normalization (train-only per-channel z-score) confirmed digit-for-digit against the actual Informer2020/LTSF-Linear data loader code.
Electricity (ECL): 7:1:2 split and normalization confirmed against Autoformer’s loader. Byte-level duplicate check against GIFT-Eval’s electricity config run this session (see “two open items” below).
Solar-Energy: 7:1:2 (LTSF-era) confirmed via loader code, but literature check found LSTNet’s original paper actually used 60/20/20 — the project’s 7:1:2 is a later drift, now noted on the card. Confirmed byte-identical to GIFT-Eval’s solar and Monash’s solar_10_minutes (found last session); this session found that exact 3-way duplication is independently published — see contamination findings below.
Traffic: 7:1:2/normalization confirmed. New finding: very likely the same underlying sensors as Monash’s traffic_hourly (same 862 sensors, same SF Bay Area window, same Lai et al. 2017 root) — not byte-verified yet, flagged for the same correlation check done for Electricity.
PEMS03/04/08: corrected this session. The prior framing (“6:2:2 graph-baseline vs 7:1:2 LTSF, pick one”) was wrong — ASTGCN, STSGCN, iTransformer, and S-Mamba all use 6:2:2 on these three datasets specifically; 7:1:2 was this project’s own import from the unrelated LTSF family. Re-standardized to store both: train_end_idx/val_end_idx/test_end_idx are now the literature-correct 6:2:2 split (primary), *_ltsf712 columns preserve the original 7:1:2 alongside. EDA re-run against the new primary split (numbers barely moved). DATASETS.md and all three cards corrected.
Weather: 7:1:2/normalization confirmed. New finding: MPI Jena’s station feed is a continuously-updated public archive going back to 2002-2003, redistributed independently many times (this LTSF slice, the separate popular “Jena Climate 2009-2016” cut, etc.) with no dedup — a plausible, previously undocumented live-feed contamination mechanism, distinct from the exact-duplicate cases found for solar/electricity. No evidence yet that any named TSFM has actually double-counted it; flagged as mechanism-level risk.
PTB-XL: fold 1-8/9/10 = train/val/test confirmed via PhysioNet’s own page, Wagner et al. 2020, and the Strodthoff et al. benchmarking repo. New contamination finding: ECG-FM pretrains on a random split of PhysioNet Challenge 2021 that includes PTB-XL without respecting PTB-XL’s own patient/fold structure — any zero-shot/fine-tune eval of an ECG-FM-style model on this project’s PTB-XL copy should be treated as contaminated unless the model authors documented an explicit exclusion.
GIFT-Eval: license and config list confirmed. The prediction-length table this project’s card had flagged as an open item (needed for any windowed eval) is now filled in from the paper’s own Table 13, cross-checked against the eval harness code. GIFT-Eval’s own paper (Aksu et al.) devotes real attention to contamination — they built a companion non-leaking pretraining corpus and quantify TimesFM/Chronos/Moirai leakage effects directly (their Appendix F) — which corroborates rather than contradicts this project’s independent electricity/solar findings.
Monash: all 20 sub-datasets now have a resolved horizon and explicit split (see “two open items” below). 9 of the 10 shipped-horizon datasets spot-checked against Godahewa et al.'s own tables, all matched.
The two specific open items from last session, now resolved:

Monash’s 10 missing horizons. Resolved via Godahewa et al.'s own frequency-based horizon rule (Section 4.1: 12 for monthly, 30 for daily, one week’s worth of steps for sub-daily), cross-checked against Chronos’s and TimesFM’s own published Monash tables where they cover the same sub-dataset: car_parts_without_missing=12, fred_md=12, hospital=12 (all triple-confirmed incl. Chronos), covid_deaths=30 (triple-confirmed), saugeenday=30 (paper+TimesFM), sunspot_without_missing=30 (paper rule only, not evaluated by Chronos/TimesFM), pedestrian_counts=168, rideshare_without_missing=168, australian_electricity_demand=336 (these three: paper+TimesFM agree, but Chronos deliberately uses a shorter horizon — 48, 24, 48 respectively — for its own benchmark, recorded but not adopted here since this project follows the archive paper’s own convention), solar_10_minutes=1008 (paper rule only). None invented — all traced to the paper’s explicit rule. Applied to standardize_monash.py (new horizon_source column distinguishes archive-shipped from literature-derived), re-standardized, re-ran EDA. Also surfaced a real tooling trap: gluonts’s own default-horizon fallback function would have silently given wrong numbers (48 instead of 168 for hourly, 60 instead of 1008 for 10-minutely) had it been trusted instead of the archive paper directly.
Electricity vs. GIFT-Eval byte-level duplicate check. Correlated all 321 of this project’s Electricity clients against all 370 of GIFT-Eval’s electricity__H clients, sliding GIFT-Eval’s series to align its last 26,304 hours (a one-year offset from its own labeled start) against ours: 297/321 (92.5%) match at correlation >0.9999, 320/321 (99.7%) match at correlation >0.999. Confirmed: same underlying UCI ElectricityLoadDiagrams client population. Not byte-identical like solar — per-pair value ratios aren’t a clean constant, consistent with different 15-min→hourly aggregation upstream. A genuinely new finding fell out of this check and could not be corroborated anywhere in the literature (checked explicitly): this project’s own electricity.parquet date column is very likely wrong. The alignment that produces the near-perfect match puts our data’s true calendar content at 2012-2014 (matching the real, documented UCI date range, and also GIFT-Eval’s own labeled dates) — but our raw CSV (an unmodified thuml/Time-Series-Library HF mirror) is dated 2016-2019. This looks like an upstream redistribution artifact, not something this project’s pipeline introduced (the CSV’s date column was carried through untouched, per the raw-immutability rule). Left as-is in datasets/processed/ for that reason, but flagged prominently since it would silently corrupt any Phase 2 pretraining-cutoff check that trusts this dataset’s timestamps at face value.
The single most important cross-cutting finding of the whole phase: a 2025 paper, Meyer et al., “Rethinking Evaluation in the Era of Time Series Foundation Models: (Un)known Information Leakage Challenges” (arXiv:2510.13654), was found independently by two different research agents this session (working on unrelated datasets) and directly corroborates and extends this project’s own from-scratch findings: it names ETTh1, ETTh2, and ETTm1 as contaminated (used in Lag-Llama pretraining and UniTime’s train/test — “cannot be benchmarked against any other model using the Informer collection”), and independently traces the exact same GIFT-Eval/Monash/LSTNet Alabama-solar lineage this project found byte-identical last session, going further to report that TinyTimeMixers and Lag-Llama were also pretrained on it and that Chronos’s own corpus separately overlaps at different resolutions. This paper is directly on-topic for this project’s entire mission and is worth reading in full before Phase 2’s leakage-matrix design starts — it may already contain a template for the kind of matrix this project intends to build.

Other real findings worth carrying forward (see individual cards for full detail): ETT/Weather/Solar-Energy train/test distribution shift; Electricity’s one all-zero-test client; Weather’s upstream column-name encoding corruption; PEMS04’s physically-sensible speed/flow inverse relationship; PTB-XL’s near-1.9%-zero-label and 23.6%-multi-label rates with balanced fold construction; Monash’s australian_electricity_demand correlated multi-state demand spike (plausible shared heatwave). PEMS-family literature criticism newly added: historical-average baselines beat learned models at H=12 on PEMS04/08 (near-random-walk-at-long-horizon concern), and the classic PeMS system paper’s ~69.6% raw sensor uptime figure (already resolved upstream in the ASTGCN-packaged .npz files, not visible in this project’s own data).

Portability audit (Step 6): found and fixed one real violation of the “only config/paths.yaml should need to change on migration” rule — standardize_monash.py had a hardcoded /workspace/Venv/TSFM/lib/python3.11/site-packages sys.path.insert, left over from early debugging and actually unnecessary since gluonts imports fine under the activated venv without it. Removed. No other hardcoded /workspace/TSFM or /workspace/Venv paths found in any script. config/paths.yaml’s existing_assets_root pointing outside the repo (to /workspace/TimeSeriesBenchMark) is intentional and by design — it’s the one pointer the WESAD symlink depends on, and it’s the only thing that would need updating for that symlink specifically when this project moves.

Total disk usage, whole phase: datasets/raw/ 1.5GB + datasets/processed/ 880MB = 2.4GB total, well within the original 6-8GB phase budget. 663GB free on /workspace as of this entry.

Blocked: None. Every dataset in DATASETS.md’s phase-scope table is [x]; nothing is [!].

New packages/scripts added: no new pip packages this session. Modified: scripts/standardize/standardize_pems.py (now writes both 6:2:2 primary and 7:1:2 alt split columns), scripts/standardize/standardize_monash.py (literature-derived horizons for the 10 previously-null sub-datasets, horizon_source column, removed the hardcoded venv path), scripts/standardize/common.py (added graph_split_6_2_2 helper), notebooks/eda/eda_gift_eval.py (added the missing solar plot). All 14 literature/notes cards updated with dated “Literature confirmation” sections.

What Phase 1 proper should pick up first, given everything learned this bridge phase:

Build the cross-dataset correlation/dedup scan used ad hoc this session (for solar and electricity) into an actual reusable Phase 2 tool, not a one-off script — run it pairwise across every dataset pair in the final Phase 2 collection before any zero-shot claim is made. This bridge phase found two confirmed duplicates and one strong suspect (Traffic vs. Monash’s traffic_hourly) just by checking the handful of pairs that seemed likely; a systematic pass across the full eventual dataset set will likely find more.
Read Meyer et al. 2025 (arXiv:2510.13654) in full before designing the Phase 2 leakage matrix — it’s independently doing close to this project’s own mission and already covers several of this project’s own datasets (ETT, solar) with specific named-model contamination claims that go beyond what this bridge phase checked (Lag-Llama, UniTime, TinyTimeMixers).
Do not trust this project’s own electricity.parquet timestamps for any pretraining-cutoff check until the true UCI ElectricityLoadDiagrams dates are independently verified — this bridge phase found strong evidence the shipped 2016-2019 label is wrong (true content is ~2012-2014) but could not find independent literature confirmation, so treat it as a project-original finding requiring its own verification pass, not an established fact to build on directly.
Treat any ECG-FM-style model’s “zero-shot” PTB-XL numbers with suspicion unless the model card explicitly documents excluding PTB-XL’s own test fold from its PhysioNet-Challenge-2021-sourced pretraining data.
PEMS03/04/08 now has both a literature-correct (6:2:2) and project-consistency (7:1:2) split stored — Phase 2 should default to 6:2:2 when comparing against any of ASTGCN/STSGCN/iTransformer/S-Mamba’s own published numbers, and only use the _ltsf712 columns when comparing against this project’s own other LTSF-family datasets for internal consistency.
Next session should start with: Phase 1 proper, once the collaborator’s compute/storage actually lands — this bridge phase’s own scope is now complete. If Phase 1 is still not available and there’s another bridge session, the two natural next steps are (a) the Traffic-vs-traffic_hourly correlation check flagged above, done the same way as the Electricity check this session, and (b) finally getting a name for the “one more existing dataset, unconfirmed” item that’s been open since the very first session (CLAUDE.md/EXISTING_ASSETS.md).

2026-07-06 (Step 3 + Step 4: standardization and EDA for all 14 datasets)

Done: Worked through all 14 datasets one at a time (standardize → immediately EDA → card → next), per this session’s explicit instruction, rather than batching. All 14 are now [x] in DATASETS.md, all have a dataset card in literature/notes/, and all have standardized parquet output in datasets/processed/ plus a runnable EDA script in notebooks/eda/. Wrote shared helpers (scripts/standardize/common.py, notebooks/eda/eda_common.py) once up front rather than duplicating logic per dataset.

Schema used: the GluonTS/uni2ts-style long schema specified this session (item_id, start, freq, target, group_id, plus dataset-specific split-index columns) for all 12 forecasting datasets; a separate custom schema for PTB-XL (record_id, patient_id, signal, lead_names, scp_codes, diagnostic_superclass, strat_fold, split) since it’s classification, not forecasting, exactly as scoped. One deliberate schema extension beyond the literal instruction: added an explicit test_end_idx column everywhere alongside the requested train_end_idx/val_end_idx, because ETT’s standard split does not run to the end of the raw array (see below) and leaving that implicit would have silently mis-defined its test set.

Split conventions applied, matching what’s standard per dataset family (not one convention forced everywhere):

ETT family: Informer’s fixed 12/4/4-month convention (not a recomputed percentage) — train_end_idx/val_end_idx/test_end_idx are 8640/11520/14400 (hourly) and 34560/46080/57600 (15-min).
Electricity/Traffic/Weather/Solar-Energy: Autoformer/Informer 7:1:2 (num_train=0.7n, num_test=0.2n, remainder=val) — test always runs to the end of the array here.
PEMS03/04/08: this was flagged in DATASETS.md as an open choice between graph-baseline 6:2:2 and LTSF-style 7:1:2 — chose 7:1:2 for project consistency, documented as a deliberate call in each card, not a default.
GIFT-Eval subset and Monash subset: used exactly what each corpus ships. GIFT-Eval’s arrow files contain full undivided series (their eval windows are procedural, not stored) so we stored the full series with no invented split. Monash ships a global @horizon for 9 of our 20 chosen sub-datasets and a genuine per-series horizon for 1 (cif_2016, matching the known CIF2016 6-vs-12-step competition design) — used those directly. The remaining 10 Monash sub-datasets ship no horizon at all; rather than guess a number from memory, left them unsplit and flagged for Step 5 literature confirmation.
PTB-XL: official strat_fold carried through untouched (not re-derived), plus a derived split convenience column (folds 1-8/9/10 → train/val/test, the standard convention). Patient-level leakage across splits explicitly asserted in code (not just eyeballed) — confirmed 0 of 18,869 patients span more than one fold.
The most important finding this session — a confirmed, not just suspected, contamination case: solar_10_minutes in the Monash subset, solar/10T in the GIFT-Eval subset, and our own directly-downloaded energy/solar_energy.parquet are byte-identical (correlation 1.0, max abs diff 0.0, checked directly, not inferred) — the same 2006 Alabama LSTNet solar dataset exists three times in this project’s own collection under three different pipelines/names. Also flagged (structurally suggestive, not value-confirmed) that GIFT-Eval’s electricity config and Monash’s electricity_weekly are each very plausibly the same UCI ElectricityLoadDiagrams source as our own electricity.parquet, just under different curation/resampling — GIFT-Eval’s copy in particular has 370 series from 2011 with genuine 19.2% missingness, vs. our 321-series, 0%-missing, 2016-onward Autoformer-cleaned copy. All three flagged prominently in the relevant cards for a proper Phase 2 leakage-matrix check, exactly the kind of issue this project’s contamination audit exists to catch.

Other real findings worth carrying forward (full detail in each card):

ETT family (all four) and Weather and Solar-Energy show real train/val/test distribution shift — ETT’s is a level/regime shift not obviously seasonal (ETTh2/ETTm2 even show sign flips on some channels), while Weather’s and Solar-Energy’s are a straightforward seasonal confound from being single-calendar-year data split chronologically (test lands in a different season than train).
Electricity has one client (electricity__182) whose entire test split is exact zeros (likely decommissioned before the test window) — needs an explicit policy (not silent inclusion) in any per-channel evaluation.
Weather’s raw CSV has upstream (not ours) encoding corruption in 3 column names — literal U+FFFD replacement characters in place of µ/² symbols, confirmed at the byte level in the source file itself, left as-is since raw data is immutable, but documented so it isn’t mistaken for a bug in our own pipeline later.
PEMS04’s speed channel shows a clean, physically-sensible inverse relationship with its flow/occupancy channels during daily congestion, which also served as a sanity check on the literature-sourced (not file-embedded) flow/occupancy/speed feature-order assumption for PEMS04/08.
PTB-XL: 1.89% of records have zero diagnostic-superclass labels (non-diagnostic-only scp_codes), 23.6% are multi-label, and class balance is nearly identical across train/val/test (confirming the stratified fold construction works as intended).
Monash’s australian_electricity_demand shows a correlated demand spike across at least 3 of its 5 state-level series at the same timestep, plausibly a shared heatwave event.
Disk usage: datasets/ is now 2.4GB total (1.5GB raw + ~900MB processed), still comfortably within the original 6-8GB phase target. 711GB free on /workspace.

Blocked: None outright. Two things explicitly left open rather than guessed at (both already flagged as literature-confirmation tasks, i.e. Step 5, not this session’s scope):

10 of the 20 Monash sub-datasets have no shipped horizon/split; need Godahewa et al.'s published horizon table checked per-dataset before any split-dependent eval.
The suspected (not yet value-confirmed) electricity duplication across GIFT-Eval/Monash/our own copy needs the same direct byte-level check that was done for solar.
New packages/scripts added: no new pip packages (everything needed — pyarrow, wfdb, gluonts, matplotlib — was already installed). New scripts: scripts/standardize/common.py (shared helpers), standardize_ett.py, standardize_wide_ltsf.py (Electricity/Traffic/Weather), standardize_solar.py, standardize_pems.py, standardize_ptbxl.py, standardize_gift_eval.py, standardize_monash.py; notebooks/eda/eda_common.py (shared helpers) plus one eda_*.py script per dataset/group, figures saved under notebooks/eda/figs/<name>/.

Next session should start with:

Step 5 (literature notes / split confirmation) is now the natural next step for all 14 — each card already has a first-pass “confirm via 1-2 papers” placeholder filled with what we have, but the explicit asks above (Monash horizons, electricity triplicate check) are the concrete starting points.
The confirmed solar triplicate (and suspected electricity duplicate) should inform how Phase 2’s leakage/contamination matrix gets designed — this project now has direct in-house evidence of the exact failure mode it’s meant to detect, worth using as a concrete worked example.
2026-07-06 (PTB-XL background download completed + verified)

Done: The PTB-XL 100Hz background download (left running at the end of the previous Step 2 session) finished cleanly. Verified completeness properly: 21,799 .hea+.dat record pairs under records100/, exact match to ptbxl_database.csv’s 21,799 data rows and to the records100 entry count in the published SHA256SUMS.txt. Ran a full sha256sum -c against all 43,598 relevant SHA256SUMS.txt entries (every records100 file plus LICENSE.txt, RECORDS, ptbxl_database.csv, scp_statements.csv) — 100% passed, zero mismatches. Final size: 705MB. DATASETS.md’s row for PTB-XL and its manifest (datasets/manifests/ptbxl.log) are both updated with the verified counts.

Running disk total: datasets/raw/ is now ~1.5GB (up from ~900MB before PTB-XL finished), still comfortably within the 6-8GB phase target. 711GB free on /workspace.

All 14 datasets in DATASETS.md’s phase-scope table are now [~] (downloaded and verified, not yet dataset-carded) — Step 2 is fully complete.

Blocked: None.

New packages/scripts added: None this entry.

Next session should start with:

Step 3 (standardization into datasets/processed/) is now unblocked for all 14 datasets, including PTB-XL — nothing is still mid-download.
Confirm with the user before starting Step 3, since prior session instructions were explicitly scoped to stop after Step 2.
2026-07-06 (Step 2: dataset downloads)

Done: 13 of 14 datasets landed cleanly and verified against expected size/row/shape; PTB-XL is still downloading in the background (not blocked, just slow — see below). Total disk used so far: ~900MB in datasets/raw/ (well within the 6-8GB phase target), 711GB free on /workspace. All statuses in DATASETS.md moved from [ ] to [~] (downloaded, not yet dataset-carded — full carding is Step 5, out of scope this session) as each landed; full details and exact commands are in datasets/manifests/<name>.log for each.

Energy + Weather group (validated the pipeline first, per STEPS.md’s ordering): ETTh1/h2, ETTm1/m2 (git clone from zhouhaoyi/ETDataset, 29MB, row counts exact match; found actual license is CC BY-ND 4.0, not CC BY/CC BY-NC as DATASETS.md hedged), Electricity (HF thuml/Time-Series-Library, 92MB, 26304×321 exact match), Solar-Energy (sparse git clone of laiguokun/multivariate-time-series-data, 8.7MB gzip, 52560×137 exact match), Weather/Jena (HF mirror, 7.0MB, 52696×21 exact match).
Traffic group: Traffic (HF mirror, 131MB, 17544×862 exact match). PEMS03/04/08 — this was a flagged pause point, so I stopped and asked before proceeding. No official/HF mirror exists (only unofficial third-party re-uploads with no checksums); the official source is Google-Drive-only per iTransformer’s GitHub README. User chose to try gdown against the official Drive bundle first — it worked on the first attempt (172MB zip, extracted just the three needed .npz files, discarded the rest of the bundle). All three shapes matched published ASTGCN paper dimensions exactly. Gotcha: this bundle has no sensor adjacency/distance files (only needed for graph-based baselines, not for TSFM point-forecast eval — flagged for later if that ever comes up).
Multi-domain group: GIFT-Eval subset (chose electricity, solar, LOOP_SEATTLE configs for Energy+Transport overlap with our Required domains; 823MB raw download shrank to 425MB after removing ~415MB of accidental upstream HF-cache duplicate files that Salesforce’s own upload left in the repo — confirmed via state.json these weren’t part of the canonical dataset). Monash Archive subset (20 datasets chosen for Chronos/TimesFM eval-suite overlap, sourced via gluonts’s own Zenodo record registry rather than hand-collecting 20 URLs, 59MB total, .tsf format confirmed parseable via gluonts’s own reader).
PTB-XL (ECG, 100Hz only): confirmed no PhysioNet login is required for this open-access project (HTTP 200, no auth wall) — so the credentials pause condition didn’t apply. Hit a transient “Unable to establish SSL connection” failure on the first two attempts at the recursive crawl (diagnosed as a network blip, not systemic — single requests and small-scope recursive tests both succeeded around it); third attempt with more generous retry/timeout settings has been running cleanly since. Still in progress at session end: PhysioNet serves each record as two tiny files (~21,837 records × 2), so this is request-latency-bound, not bandwidth-bound — only ~1,728 records (58MB) landed in this session’s window, extrapolating to several more hours to finish. Left running in the background since it isn’t blocked, just slow (per instruction, PTB-XL was only a pause point if credentials were the issue). Spot-checked a landed record’s .hea header: correctly formed, 12-lead, 100Hz, 1000 samples — legitimate data, not corrupted.
Blocked: Nothing hard-blocked. PTB-XL is soft-blocked only in the sense that it needs more wall-clock time; it isn’t stuck.

New packages/scripts added: None new this session (everything needed was installed in the prior Step 0 session). Discovered HF_HUB defaults its cache to /root/.cache/huggingface rather than the project’s configured huggingface_cache path — set HF_HOME=/workspace/TSFM/.cache/huggingface explicitly for every HuggingFace download this session to match config/paths.yaml. Worth baking into any future download scripts under scripts/download/ rather than relying on remembering to export it by hand.

Next session should start with:

Check whether the PTB-XL background wget finished (du -sh datasets/raw/ecg/ptbxl, compare record count to ptbxl_database.csv’s ~21,837, verify against the published SHA256SUMS.txt per DATASETS.md’s own instruction). If it stalled or died, just restart the same wget -r -N -c -np -X .../records500 ... command — -c makes it resume rather than restart from scratch.
Once PTB-XL is confirmed complete, all 14 datasets are downloaded — Step 2 will be fully done and Step 3 (standardization into datasets/processed/) can start.
Do NOT start Step 3 on the other 13 datasets prematurely without checking with the user first, even though they’re individually ready — the user’s instruction this session was to stop after Step 2 and report back.
2026-07-06 (Step 0 + Step 1)

Done:

Step 0 (scaffolding): confirmed venv (/workspace/Venv/TSFM, Python 3.11.15, uv working). Installed missing packages via uv pip install: wfdb, openpyxl, matplotlib, seaborn, ipykernel, gluonts, gdown (pyarrow, tqdm, requests were already present from an earlier session). Created the full working directory tree (config/, datasets/raw/{energy,traffic,weather,ecg,multi_domain,wearables}/, datasets/processed/{...}/, datasets/manifests/, notebooks/eda/, literature/notes/, scripts/download/, scripts/standardize/). Added a wearables/ subdir to raw/processed beyond the literal STEPS.md list since DATASETS.md already designates datasets/raw/wearables/wesad/ as WESAD’s symlink target. config/paths.yaml already existed from the prior session; added the one missing key (data_root: /workspace/TSFM/datasets) so it matches STEPS.md’s minimum key list, then validated it parses cleanly with yaml.safe_load.
Step 1 (existing asset inventory): thoroughly inspected /workspace/TimeSeriesBenchMark/ (not just directory names — actual file listings and formats). Full writeup now in EXISTING_ASSETS.md. Headline results:
MIMIC-IV confirmed clinical, not ECG waveform. physionet.org/files/mimiciv/3.1/{hosp,icu}/ contains only .csv.gz tabular files (admissions, labevents, chartevents, icustays, etc), ~10GB total, no WFDB .dat/.hea pairs anywhere. This matches what CLAUDE.md/EXISTING_ASSETS.md already assumed, now verified directly rather than taken on faith.
WESAD confirmed genuine and complete at /workspace/TimeSeriesBenchMark/datasets/WESAD/ (19GB, subjects S2-S17 excluding S12, matching the official WESAD exclusions exactly). Symlinked into datasets/raw/wearables/wesad/. Noted one stray unidentified 2.2GB zip (HGdUkoNlW1Ub0Gx) sitting in the WESAD root outside the per-subject folders — left untouched, doesn’t affect usability.
Found derived-but-irrelevant MIMIC-IV artifacts (datasets/MIMIC4_causal, MIMIC4_processed [82,421 parquet files], MIMIC4_qa, MIMIC4_features) belonging to a separate, unrelated causal-discovery research project on this server (akshat/causalTS/dysts/kd/old, ~108GB combined) — synthetic dynamical systems, simglucose CGM simulator, MIMIC causal-QA generation. Confirmed via targeted search that none of our Minimal/Recommended tier targets (ETT, Electricity, Traffic, Weather, PEMS03/04/08, Solar-Energy, PTB-XL, GIFT-Eval, Monash) exist anywhere on this server — every one of those is a fresh Step 2 download.
Cross-referenced against DATASETS.md’s “Deferred / already present” table: it was already accurate, no edits needed there.
Blocked:

None. Both steps completed cleanly.
New packages/scripts added:

wfdb, openpyxl, matplotlib, seaborn, ipykernel, gluonts, gdown via uv pip install. Side effect: installing gluonts pulled pandas down from 3.0.3 to 2.3.3 (a gluonts dependency constraint) — re-verified all core imports (torch, datasets, huggingface_hub, transformers, pandas, numpy) still work fine together after the downgrade.
Next session should start with:

Step 2 in STEPS.md (download Minimal-tier datasets first: ETT family, Electricity, Weather, Traffic — smallest and most reliable, good for validating the download/standardize pipeline before PTB-XL and the multi-domain corpora).
Two things flagged for the user before Step 2 gets to them: (1) the “one more existing dataset, name unconfirmed” mentioned in CLAUDE.md is still not identified — /workspace/ has several other lab members’ project directories one level up from TimeSeriesBenchMark/ (Emotiv, Brain, SensorLLM, GlycoLM, TSZechen, etc.) that were noticed in passing but not inventoried, since that’s outside this session’s TimeSeriesBenchMark/-scoped Step 1 — see EXISTING_ASSETS.md’s “Discovered but out of scope” section; (2) PEMS03/04/08 and PTB-XL credentialing/access are still untouched per the user’s explicit request to pause on those rather than proceed unilaterally.
2026-07-06 (phase kickoff)

Done:

Reviewed all project planning documents (documents/), the dataset/model catalog, and confirmed the phase scope with the user: data-only, Minimal tier plus light Recommended additions (Solar-Energy, PEMS03/04/08, GIFT-Eval subset, Monash subset), no model runs this phase.
Confirmed with the user that MIMIC-IV-ECG credentialing is not being started yet, because the server already has some existing assets (MIMIC-IV clinical hosp/icu tables, WESAD, at least one more unconfirmed) that need to be inventoried first. Full list still pending from the user.
Created CLAUDE.md, PROJECT.md, BENCHMARK_SPEC.md, DATASETS.md, STEPS.md, this file, and config/paths.yaml.example as the working context set.
Blocked:

Full inventory of existing server assets not yet available, EXISTING_ASSETS.md is currently a stub. Step 1 in STEPS.md needs to happen before Step 2 downloads anything that might already exist.
New packages/scripts added:

None yet, packages listed in CLAUDE.md (wfdb, pyarrow, tqdm, requests, openpyxl, matplotlib, seaborn, jupyter/ipykernel, gluonts, gdown) still need to be installed.
Next session should start with:

Step 0 (scaffolding, config/paths.yaml, package install) and Step 1 (existing asset inventory) from STEPS.md, in that order, before touching any download commands in DATASETS.md.
