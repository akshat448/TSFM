# EXISTING_ASSETS.md

Inventory of datasets and other relevant assets that already exist somewhere on this server, so `DATASETS.md` downloads never duplicate something we already have.

Status: **Step 1 complete for `/workspace/TimeSeriesBenchMark/` (2026-07-06) and for the five `CLAUDE.md`-deferred lab directories — `Emotiv`, `Brain`, `SensorLLM`, `GlycoLM`, `TSZechen` (2026-07-08).** Everything below was confirmed by directly inspecting file contents/formats, not guessed from folder names. None of the five lab directories overlap this project's scope (all EEG/HAR/CGM, already-excluded domains). "engauge" was searched for explicitly and not found — see that section below. A handful of other `/workspace/` top-level directories noticed in passing (`Opportunity++.zip`, `Eye`, `CVTan`, `Zara`, `QueryAdapt`, `PanOptic_Seek`, `AutoMorph`, `Sanskrit`, `world-cont`) remain out of scope — nobody has asked for those yet.

**NOTE (2026-07-30): this whole file describes the OLD server (`/workspace/TimeSeriesBenchMark/`).** None of these paths exist on the new server (`/mnt/hdd1/TSFM/`). Kept here for historical record (WESAD needed a real copy, not a symlink, during migration — see `PATHS_AND_MIGRATION.md`). A fresh inventory of the new server is a separate, not-yet-done task — see `NEXT_PHASE_TASKS.md` task A0.

## Confirmed assets relevant to this project

| Asset | Location | What it actually is | Confirmed? | Relevant to which `DATASETS.md` entry |
|---|---|---|---|---|
| MIMIC-IV clinical (hosp/icu) | `/workspace/TimeSeriesBenchMark/physionet.org/files/mimiciv/3.1/{hosp,icu}/` | **MIMIC-IV clinical**, NOT the ECG waveform database. All files are `.csv.gz` tabular EHR tables (admissions, diagnoses, labs, prescriptions, chartevents, icustays, etc). No WFDB `.dat`/`.hea` pairs anywhere in the tree. `SHA256SUMS.txt`, `LICENSE.txt`, `CHANGELOG.txt` present alongside. | **Yes**, confirmed by direct file listing and format inspection (see method below) | None directly for the ECG domain. Genuinely useful for the project's Phase 3 EHR direction, per `CLAUDE.md` known open items. Does **not** substitute for MIMIC-IV-ECG (waveform DB), which is a separate PhysioNet product needing its own credentialing. |
| WESAD | `/workspace/TimeSeriesBenchMark/datasets/WESAD/` | Genuine, complete WESAD dataset. 15 subject folders (`S2`-`S17`, excluding `S1` and `S12` — matches the official WESAD subject exclusions exactly). Each subject folder has `<S>.pkl` (main synchronized data), `<S>_E4_Data.zip` (Empatica E4 wrist device raw), `<S>_quest.csv` (questionnaire), `<S>_readme.txt`, `<S>_respiban.txt` (chest RespiBAN raw). Plus a top-level `wesad_readme.pdf`. | **Yes**, confirmed by direct inspection, subject count/naming matches published WESAD structure | Wearables tier, deferred this phase per `DATASETS.md`. Symlinked (see below), no fresh download needed. |
| Derived MIMIC-IV artifacts (`MIMIC4_causal`, `MIMIC4_processed`, `MIMIC4_qa`, `MIMIC4_features`) | `/workspace/TimeSeriesBenchMark/datasets/MIMIC4_*` | Derived outputs from a **different, unrelated research project** on this server (a causal-discovery time-series benchmark, "causalTS"/"akshat"/"rhea"), built on top of the same MIMIC-IV clinical tables above. `MIMIC4_processed/` has 82,421 per-admission `.parquet` files; `MIMIC4_qa/` has a QA-pair jsonl + summary csv; `MIMIC4_features/` has an aggregated features csv; `MIMIC4_causal` has small causal-discovery result jsons. | Yes, inspected directly | Not usable for this project — these are that other project's derived artifacts, not raw MIMIC-IV-ECG and not standardized to our conventions. Noted for completeness only. |

## Confirmed present but NOT relevant to this benchmark (different project, same server)

`/workspace/TimeSeriesBenchMark/{akshat, causalTS, dysts, kd, old}` are all a **separate, unrelated research effort** by other lab members — a causal-discovery benchmark over synthetic dynamical systems (Lorenz, Rössler, Halvorsen, Rucklidge, Chen, Hindmarsh-Rose, PDE-Burgers, etc.), `simglucose` (a synthetic CGM/glucose simulator), and MIMIC-derived causal QA generation notebooks (`old/rhea/`). Sizes: `akshat/` ~105GB (mostly nested `.venv` installs and experiment results/checkpoints), `causalTS/` ~3.1GB, `dysts/` ~50MB, `kd/` ~91MB, `old/` ~337MB.

None of this overlaps with our Minimal/Recommended tier target datasets. I explicitly searched the entire `/workspace/TimeSeriesBenchMark/` tree for ETTh/ETTm, `electricity`, `traffic.csv`, `weather.csv`, `PEMS0*`, `solar`, `ptbxl`/`ptb-xl`, `gift`, `monash`, and `.tsf` files — the only hits were unrelated library source files inside nested `.venv` site-packages (e.g. a `gluonts` internal `electricity.py` helper, matplotlib's `Solarize_Light2` style). **No actual ETT/Electricity/Traffic/Weather/PEMS/Solar-Energy/PTB-XL/GIFT-Eval/Monash data exists anywhere on this server.** All of these remain fresh-download tasks for Step 2.

## Gotcha found

`/workspace/TimeSeriesBenchMark/datasets/WESAD/HGdUkoNlW1Ub0Gx` — an oddly-named (looks like a random download-tool-generated name) 2.2GB zip file sitting directly in the WESAD root, not part of the standard WESAD layout. Left untouched (raw data is immutable per `CLAUDE.md` rule 5, and this sits outside our symlink target's per-subject folders anyway). Cause unknown; doesn't block using the 15 subject folders, which are complete and correctly named.

## Symlinks created this session (OLD SERVER — no longer applicable, see migration note above)

```
/workspace/TSFM/datasets/raw/wearables/wesad -> /workspace/TimeSeriesBenchMark/datasets/WESAD
```

## Method used to confirm MIMIC-IV identity

Per `STEPS.md` Step 1: checked the actual file listing rather than assuming from the directory name. `find /workspace/TimeSeriesBenchMark/physionet.org -maxdepth 5` shows only `hosp/*.csv.gz` and `icu/*.csv.gz` files (admissions, patients, labevents, chartevents, icustays, etc.) — the defining signature of MIMIC-IV **clinical**. The ECG waveform product (MIMIC-IV-ECG) would show up as WFDB `.dat`/`.hea` record pairs, which are entirely absent from this tree.

## `/workspace/` lab directories one level up from `TimeSeriesBenchMark/` — inventoried 2026-07-08

The deferral on `Emotiv`, `Brain`, `SensorLLM`, `GlycoLM`, `TSZechen` (named explicitly in `CLAUDE.md`'s "still skip" list) was lifted this session. Actually opened each one and inspected real file listings/formats, not just directory names — same standard as the `TimeSeriesBenchMark/` inventory above. **None of these overlap with this project's Minimal/Recommended tier scope** (ETT, Electricity, Traffic, Weather, PEMS03/04/08, Solar-Energy, PTB-XL, GIFT-Eval, Monash) — they're EEG, HAR/wearables, and CGM research, domains `CLAUDE.md` already lists as out of scope for this phase. Nothing was downloaded, symlinked, or copied — inventory only, per this session's instruction.

**One item flagged and left untouched, not inspected further:** `/workspace/SensorLLM/vaibhav/FOOD_CGM/.env` — a file of this name conventionally holds API keys/secrets. Not opened. Confirmed with the user mid-session to skip it and continue the rest of the inventory around it; nothing about its contents is documented here.

| Directory | Total size | What it actually is |
|---|---|---|
| `Emotiv` | 188GB | EEG decoding / brain-to-text research, unrelated to this project. `brennan_dataset/` is the published Brennan & Hale naturalistic-reading EEG corpus (BrainVision `.eeg`/`.vhdr`/`.vmrk` per-subject files, S01-S49ish, `AliceChapterOne-EEG.csv`, its own `LICENSE.txt`). `ear_dataset/` is an apparently custom lab EEG experiment (`Data/eeg/`, `Data/behavior/datakey_sub-NN.csv` per-subject files, demographics CSV, audio stimuli) with its own `CLAUDE.md`, suggesting active/recent work. `brainecho/` and `reve_eeg/` are EEG-to-text model training code (checkpoints, `.git` repos, one with its own `CLAUDE.md` and `.venv`). `Downloads.tar.gz` (9.7GB) and `OldWork.tar.gz` (32GB) are unopened archives, likely old backups — not extracted, contents unknown beyond the name. |
| `Brain` | 882GB | Also EEG/brain-decoding research, appears to be a related or overlapping effort to `Emotiv` (different lab members, similar domain). `datasets/CHISCO*` and `datasets/clean-chisco/` are the published CHISCO imagined-speech EEG dataset from OpenNeuro (`ds005170-download`, `ds006317-download` — proper BIDS structure: `sub-01`...`sub-05`, `participants.tsv`, `dataset_description.json`, DataLad-managed). `ica-v-nica/` is a small (9.1MB) ICA-vs-something artifact-removal comparison. `old/ICML 2026/` contains multiple lab members' individual experiment subdirectories (`kanav_exps`, `akshat_experiments`, `Tanmay_exp`, etc.) — code/notebooks, not raw data. `old/` (414GB) and `datasets/` (370GB) and `loss-stability/` (97GB) account for nearly all the size; not exhaustively catalogued below the second level given the scale, but nothing beyond EEG-domain research surfaced. |
| `SensorLLM` | 8.0GB (small) | LLM-for-sensor-data reasoning experiments. Top-level scripts (`Classcollect.py`, `QADat.py`, `ReasoningVeracity.py`) process "merged CSVs" of activity classes for QA-style dataset construction (referenced paths like `merged_CSVs/`, `csv_converted/`, and a `physionet.org/files/scientisst-move-biosignals/...` path in `preprocess.py` do **not** currently exist on disk — referenced by code but not present, so whatever that pulled from ScientISST Move's open wearable-biosignals PhysioNet dataset isn't actually here anymore). `vaibhav/` (8.0GB) holds `FOOD_CGM`/`FOOD_CGM_COT` subdirectories — the same CGM-plus-food-log project structure found in `GlycoLM/CGM/` (see below), i.e. this looks like shared/duplicated work between the two directories, not two independent datasets. Also contains unrelated image-generation experiments (`tree-ring-watermark-main`, Blender, a car PNG) — a personal/exploratory scratch area, not a coherent single dataset. |
| `GlycoLM` | 65GB | Continuous glucose monitoring (CGM) research. `physionet.org/files/big-ideas-glycemic-wearable/1.1.2/` (the bulk of the 65GB, ~35GB) is the genuine, published, open-access PhysioNet "BIG IDEAs" glycemic + wearable dataset — 16 subjects, `Dexcom_NNN.csv`/`ACC_NNN.csv`/`BVP_NNN.csv`/`EDA_NNN.csv`/`HR_NNN.csv`/`TEMP_NNN.csv`/`IBI_NNN.csv`/`Food_Log_NNN.csv` per subject, plus an aggregate `Demographics.csv` (ID/Gender/HbA1c only — no names or other identifiers), `LICENSE.txt` (ODC-By), `SHA256SUMS.txt` — standard PhysioNet open-access distribution, same style of dataset as WESAD/PTB-XL already in this project's scope, just a different domain. `CGM/`, `CGMformer/` (a cloned GitHub model repo), `cgm_ckp*` (17-28MB, trained model checkpoints — `pytorch_model.bin` etc., not data) are modeling work built on top of it. `rhea_exps/` (31GB) is unrelated: old SemEval word-sense-disambiguation BERT fine-tuning experiments (checkpoints, a Python venv) — looks misplaced/mislabeled into this directory rather than genuinely CGM-related. |
| `TSZechen` | 2.0TB (by far the largest) | Human Activity Recognition (HAR) + LLM fusion research, the most active-looking of the five by directory count and naming (many dated experiment-run folders). `Data/` contains several well-known **published, open** HAR benchmark datasets: `UCIHar` (UCI HAR), `PAMP2` (PAMAP2), `opportunity` (the OPPORTUNITY Activity Recognition dataset — this is the "contains an opportunity/ subdir" already anticipated in this file's previous stub entry, now confirmed directly), `capture24`, `hhar` (Heterogeneity Activity Recognition), `USC-HAD`, and a `WISDM_at_latest.tar.gz`. `NymeriaVeracity` (22GB) refers to Meta's Nymeria egocentric motion-capture dataset. The rest of the 2TB is almost entirely published HAR model implementations (`DeepConvLSTM`, `Attend-And-Discriminate`, `HARMAMBA`, `InceptionTime`, `CrossHAR`, `UniMTS`, `opentslm`) and large LLM+sensor-fusion experiment output directories (`crossattn_*_llama*/gemma*/qwen*_...` — cross-attention sensor-to-LLM training runs, one alone is 18GB) plus a `SensorLLM/` subdirectory (16GB) that appears to be a separate/parallel copy of similar CGM+LLM work to the top-level `/workspace/SensorLLM/` directory. Given the scale, individual multi-terabyte subdirectories (`AtteFinalPipeline` 1.4TB, `Contrastive Baseline` 419GB) were sized but not exhaustively catalogued file-by-file — nothing in a reasonable-depth listing suggested anything beyond HAR/wearables-domain research and LLM training artifacts. |

**"engauge" — searched explicitly this session, genuinely not found.** Ran a case-insensitive filename search for `engauge`, `ingauge`, `ingage`, and `engage` across all five directories (excluding `.venv`/`node_modules`/`site-packages` noise) — zero matches. None of the actual dataset names found (Brennan, CHISCO, ear_dataset, reve_eeg, big-ideas-glycemic-wearable, ScientISST Move, UCI HAR, PAMAP2, Opportunity, Capture-24, HHAR, USC-HAD, WISDM, Nymeria) is phonetically or structurally close to "engauge" either. This remains genuinely unresolved — not a matter of insufficient searching, the term simply doesn't appear to correspond to anything in these five directories. If it refers to something else entirely (a different location, a dataset not yet downloaded anywhere on this server, or a mishearing/typo of a name not covered here), that needs the user's direct input; guessing further risks documenting a wrong match.

**Still not inventoried, out of this session's explicit scope:** the other `/workspace/` top-level directories noted in passing during the original Step 1 pass (`Opportunity++.zip`, `Eye`, `CVTan`, `Zara`, `QueryAdapt`, `PanOptic_Seek`, `AutoMorph`, `Sanskrit`, `world-cont`) — this session's deferral-lift was specific to the five named in `CLAUDE.md`, not this longer list. `Opportunity++.zip` in particular is worth a name-only note: it likely refers to the same OPPORTUNITY HAR dataset family found in `TSZechen/Data/opportunity`, but wasn't opened to confirm.

## Action items

- [x] MIMIC-IV nature confirmed: clinical (hosp/icu), not ECG waveform.
- [x] WESAD path confirmed and symlinked.
- [x] Cross-referenced against `DATASETS.md`'s "Deferred / already present" table — no changes needed there, it already correctly describes both assets.
- [x] Confirmed none of the Minimal/Recommended tier download targets already exist on this server.
- [x] Inventoried `Emotiv`, `Brain`, `SensorLLM`, `GlycoLM`, `TSZechen` (2026-07-08) — all EEG/HAR/CGM research, none overlap this project's Minimal/Recommended tier scope, nothing downloaded or symlinked.
- [x] Searched explicitly for "engauge" across those five directories (2026-07-08) — not found, genuinely unresolved, not a search-thoroughness gap.
- [ ] The remaining `/workspace/` top-level directories (`Opportunity++.zip`, `Eye`, `CVTan`, `Zara`, `QueryAdapt`, `PanOptic_Seek`, `AutoMorph`, `Sanskrit`, `world-cont`) are still uninventoried and out of scope until the user asks for them specifically.
- [ ] **NEW (2026-07-30):** inventory the new server (`/mnt/hdd1/`) from scratch — nothing above applies to it. See `NEXT_PHASE_TASKS.md` task A0 / `CODEX_PROMPTS.md` Prompt 1.
