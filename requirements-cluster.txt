# Cluster/GPU training requirements for scripts/train_cluster.py.
# torch itself is installed separately in scripts/_train_worker.sh with the
# cluster's CUDA version pinned -- not listed here to avoid pip grabbing a
# mismatched build.

numpy
scipy
pandas
transformers
wandb
python-dotenv

# For chisco_pipeline/raw_dataloader.py (raw .edf loading, no preprocessing):
# mne reads/epochs the .edf files; openpyxl is pandas' engine for the
# textdataset .xlsx label files.
mne
openpyxl

# CUDA-only, used when --mamba-backend mamba_ssm (see chisco_pipeline/encoder.py).
# Both need a CUDA toolchain to build and will fail to install on CPU-only
# machines (e.g. this local dev machine) -- that's expected, leave commented
# out unless you're actually building on a CUDA box.
# causal-conv1d>=1.4.0
# mamba-ssm>=2.2.0
